import 'package:flutter/material.dart';

class AddUserInfoToFirestore extends StatelessWidget {
  const AddUserInfoToFirestore({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("adduserinfotofirestore page"),
      ),
    );
  }
}
